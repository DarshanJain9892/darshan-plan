## command for fetch_sha-id script

#if default input file (servers.txt) is to be used
python3 fetch_sha-id.py

# if seperate input file is there
python3 fetch_sha-id.py input.txt

----------------------------------------------------------------

## command for compare_sha_output script 

this scripts need input which is the output of fetch_sha-id.py script in format (ip,tomcatname,appname,shaid)

python3 compare_sha_outputs.py <input_for_target.csv> <input_for_refrence.csv>